# Architecture Probes

Use this top-level directory for temporary or checked-in architecture checker probes.
Do not place probe domains under `domain/`; that tree is reserved for application
domains.
